# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Add Linux headless packaging scripts and systemd unit
- Add `HASS.Agent.Headless` binary target and small `SimpleHeadless` runner
- Add `README-linux.md` with build/install instructions
- Add unit tests for `LegacyCommandMapper` and mapping improvements
- Add manual `.deb` creation fallback in `deploy/build_deb.sh`
